- index.html with a div with id 'app'
- script tag linking html to scripts/app.js
- terminal. nav to project folder
- [ live-server public ]
- set up babel for compiling react/ecma6/7 to ecma5
- [ babel src/app.js --out-file=public/scripts/app.js --presets=env,react --watch
- in src/app.js, follow your react with
    - const appRoot = document.getElementById('app')
    - ReactDOM.render(template, appRoot)
    - where template is like:
        - const template = (
            <div>
                <h1> JSX react style </h1>
            </div>
